import Deploy from "@/components/Deploy";

export const metadata = { title: "Deploy an agent — Aioxy" };

/** Pick a desk, grant it order rights. The only place a delegation is created. */
export default function Page() {
  return <Deploy />;
}
